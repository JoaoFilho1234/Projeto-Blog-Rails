
<div class="row g-5">
    <div class="col-md-8">
      <h3 class="pb-4 mb-4 fst-italic border-bottom">
        Bem Vindo ao Nerdologia!
      </h3>
  
    <%= link_to 'Novo Post', new_post_path,  class: 'btn btn-outline-primary' if user_signed_in? %>

    <%= render @posts%>
    <%= render 'layouts/pagination' %>   
    </div>
    <%= render 'layouts/about'%>
</div>

